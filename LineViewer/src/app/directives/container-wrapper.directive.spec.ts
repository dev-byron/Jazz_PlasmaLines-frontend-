import { ContainerWrapperDirective } from './container-wrapper.directive';

describe('ContainerWrapperDirective', () => {
  it('should create an instance', () => {
    const directive = new ContainerWrapperDirective();
    expect(directive).toBeTruthy();
  });
});
