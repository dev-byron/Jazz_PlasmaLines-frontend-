const mongoose = require('mongoose');
const Schema = mongoose.Schema;

let LineConfigurationSchema = new Schema({
    
});

// Export the model
module.exports = mongoose.model('LineConfiguration', LineConfiguration);